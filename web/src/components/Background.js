import styled from 'styled-components';

const Background = styled.div`
  background: linear-gradient(to bottom, #00657e 0%, #003366 100%) fixed;
  color: #fff;
  min-height: 100vh;
  overflow: hidden;
  position: relative;
  display: flex;
`;

export default Background;
